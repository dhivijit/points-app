<!DOCTYPE html>
<html>

<head>
    <title>Home</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
</head>

<body class="d-flex vh-100">
    <div class="container my-auto">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6 text-center px-3">
                <% if (authenticated) { %>
                    <h1 class="mb-4">Welcome</h1>
                    <div class="d-grid gap-3">
                        <a href="/leaderboard" class="btn btn-lg btn-primary">Leaderboard</a>
                        <a href="/assign" class="btn btn-lg btn-secondary">Assign Points</a>
                        <form method="post" action="/logout">
                            <button class="btn btn-lg btn-danger w-100">Logout</button>
                        </form>
                    </div>
                <% } else { %>
                    <h1 class="mb-4">Login</h1>
                    <% if (error) { %>
                        <div class="alert alert-danger"><%= error %></div>
                    <% } %>
                    <form method="post" action="/" class="mx-auto" style="max-width:400px;">
                        <div class="mb-3">
                            <input type="password" name="password" class="form-control form-control-lg" placeholder="Password" required>
                        </div>
                        <button type="submit" class="btn btn-primary btn-lg w-100">Login</button>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
